# Nutan Bisandre — Portfolio (Editorial theme)

Single-page static portfolio. Warm paper tones, light/dark toggle, serif headings, subtle grain texture.

## Local preview
Just open `index.html` in a browser, or run:
```bash
npx serve .
```
